use ("Hamburgueseria")
db.Hamburguesas.aggregate([
    {
        $unwind: "$ingredientes"
    },
    {
        $group: {
            "_id": "$chef",
            "cantidad_ingredientes": { $sum: 1 }
        }
    },
    {
        $sort: { "cantidad_ingredientes": -1 }
    }
])